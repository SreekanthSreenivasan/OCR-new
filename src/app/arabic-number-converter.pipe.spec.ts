+import { ArabicNumberConverterPipe } from './arabic-number-converter.pipe';

describe('ArabicNumberConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArabicNumberConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
